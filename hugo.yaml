# Mounts
module:
  mounts:
    - source: content
      target: content
    - source: layouts
      target: layouts

# Set brand
params:
  brand:
    email: contact@uncinq.dev
    name: Un Cinq
    url: uncinq.dev
